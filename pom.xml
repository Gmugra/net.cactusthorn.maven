<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>net.cactusthorn.maven</groupId>
	<artifactId>root</artifactId>
	<version>0.1</version>
	<packaging>pom</packaging>
	<name>Maven Template</name>
	<description>Maven tips and tricks</description>
	<licenses>
		<license>
			<name>BSD 2-Clause License</name>
			<url>http://opensource.org/licenses/BSD-2-Clause</url>
			<distribution>repo</distribution>
		</license>
	</licenses>
	<developers>
		<developer>
			<name>Alexei Khatskevich</name>
		</developer>
	</developers>
	<repositories>
		<repository>
			<id>project-maven-repository</id>
			<name>The project local maven repository</name>
			<url>file:///${project.basedir}/project-maven-repository</url>
			<releases>
				<enabled>true</enabled>
				<checksumPolicy>ignore</checksumPolicy>
				<updatePolicy>always</updatePolicy>
			</releases>
			<snapshots>
				<enabled>true</enabled>
				<checksumPolicy>ignore</checksumPolicy>
				<updatePolicy>always</updatePolicy>
			</snapshots>
		</repository>
	</repositories>
	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

		<java.version>1.8</java.version>
		<require.maven.version>3.3.1</require.maven.version>

		<dependency.version.spotbugs>3.1.6</dependency.version.spotbugs>
		<dependency.version.checkstyle>8.12</dependency.version.checkstyle>
		<dependency.version.junit>4.12</dependency.version.junit>
		<dependency.version.slf4j-api>1.7.25</dependency.version.slf4j-api>
		<dependency.version.logback-classic>1.2.3</dependency.version.logback-classic>

		<dependency.version.net-cactusthorn-utils>0.1</dependency.version.net-cactusthorn-utils>

		<plugin.version.maven.compiler>3.8.0</plugin.version.maven.compiler>
		<plugin.version.maven.source>3.0.1</plugin.version.maven.source>
		<plugin.version.build.helper.maven>3.0.0</plugin.version.build.helper.maven>
		<plugin.version.maven.surefire>2.18.1</plugin.version.maven.surefire>
		<plugin.version.maven.failsafe>2.18.1</plugin.version.maven.failsafe>
		<plugin.version.maven.checkstyle>3.0.0</plugin.version.maven.checkstyle>
		<plugin.version.spotbugs.maven>3.1.3</plugin.version.spotbugs.maven>
		<plugin.version.maven.shade>3.1.1</plugin.version.maven.shade>
		<plugin.version.maven.jar>3.1.0</plugin.version.maven.jar>
		<plugin.version.maven.enforcer>1.4.1</plugin.version.maven.enforcer>
	</properties>
	<profiles>
		<profile>
			<id>main</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<properties>
				<build.profile.id>main</build.profile.id>
				<skipIntegrationTests>true</skipIntegrationTests>
				<skipTests>false</skipTests>
				<logback-configurationFile>dummy.xml</logback-configurationFile>
			</properties>
		</profile>
		<profile>
			<id>test-quiet</id>
			<properties>
				<build.profile.id>test-quiet</build.profile.id>
				<skipIntegrationTests>true</skipIntegrationTests>
				<skipTests>false</skipTests>
				<logback-configurationFile>only-error-logback-test.xml</logback-configurationFile>
			</properties>
		</profile>
		<profile>
			<id>integration-test</id>
			<properties>
				<build.profile.id>integration-test</build.profile.id>
				<skipIntegrationTests>false</skipIntegrationTests>
				<skipTests>true</skipTests>
				<logback-configurationFile>dummy.xml</logback-configurationFile>
			</properties>
		</profile>
		<profile>
			<id>integration-test-quiet</id>
			<properties>
				<build.profile.id>integration-test-quiet</build.profile.id>
				<skipIntegrationTests>false</skipIntegrationTests>
				<skipTests>true</skipTests>
				<logback-configurationFile>only-error-logback-test.xml</logback-configurationFile>
			</properties>
		</profile>
	</profiles>
	<modules>
		<module>build-tools</module>
		<module>submodule-one</module>
		<module>submodule-pom</module>
	</modules>
	<dependencies>
		<!-- dependency from project-maven-repository -->
		<dependency>
			<groupId>net.cactusthorn</groupId>
			<artifactId>utils</artifactId>
			<version>${dependency.version.net-cactusthorn-utils}</version>
		</dependency>
		<!-- to make maven-build-tools available in surefire/failsafe-plugin classpath -->
		<dependency>
			<groupId>net.cactusthorn.maven</groupId>
			<artifactId>build-tools</artifactId>
			<version>${project.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>${dependency.version.junit}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
			<version>${dependency.version.slf4j-api}</version>
		</dependency>
		<dependency>
			<groupId>ch.qos.logback</groupId>
			<artifactId>logback-classic</artifactId>
			<version>${dependency.version.logback-classic}</version>
		</dependency>
	</dependencies>
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>${plugin.version.maven.compiler}</version>
				<configuration>
					<source>${java.version}</source>
					<target>${java.version}</target>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-enforcer-plugin</artifactId>
				<version>${plugin.version.maven.enforcer}</version>
				<executions>
					<execution>
						<id>enforce-maven</id>
						<goals>
							<goal>enforce</goal>
						</goals>
						<configuration>
							<rules>
								<requireMavenVersion>
									<version>${require.maven.version}</version>
								</requireMavenVersion>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>${plugin.version.maven.jar}</version>
				<configuration>
					<archive>
						<manifest>
							<addDefaultImplementationEntries>true</addDefaultImplementationEntries>
						</manifest>
					</archive>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<version>${plugin.version.maven.source}</version>
				<executions>
					<execution>
						<id>attach-sources</id>
						<phase>verify</phase>
						<goals>
							<goal>jar-no-fork</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>build-helper-maven-plugin</artifactId>
				<version>${plugin.version.build.helper.maven}</version>
				<executions>
					<execution>
						<id>add-generate-sources</id>
						<phase>generate-sources</phase>
						<goals>
							<goal>add-source</goal>
						</goals>
						<configuration>
							<sources>
								<source>generated-src/main/java</source>
							</sources>
						</configuration>
					</execution>
					<execution>
						<id>add-generate-resources</id>
						<phase>generate-resources</phase>
						<goals>
							<goal>add-resource</goal>
						</goals>
						<configuration>
							<resources>
								<resource>
									<directory>generated-src/main/resources</directory>
								</resource>
							</resources>
						</configuration>
					</execution>
					<execution>
						<id>add-integration-test-sources</id>
						<phase>generate-test-sources</phase>
						<goals>
							<goal>add-test-source</goal>
						</goals>
						<configuration>
							<sources>
								<source>src/integration-test/java</source>
							</sources>
						</configuration>
					</execution>
					<execution>
						<id>add-integration-test-resources</id>
						<phase>generate-test-resources</phase>
						<goals>
							<goal>add-test-resource</goal>
						</goals>
						<configuration>
							<resources>
								<resource>
									<directory>src/integration-test/resources</directory>
								</resource>
							</resources>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>${plugin.version.maven.surefire}</version>

				<configuration>
					<skipTests>${skipTests}</skipTests>
					<excludes>
						<exclude>**/*IT.java</exclude>
						<exclude>**/IT*.java</exclude>
						<exclude>**/*ITCase.java</exclude>
					</excludes>
					<systemPropertyVariables>
						<logback.configurationFile>${logback-configurationFile}</logback.configurationFile>
					</systemPropertyVariables>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<version>${plugin.version.maven.failsafe}</version>
				<executions>
					<execution>
						<goals>
							<goal>integration-test</goal>
							<goal>verify</goal>
						</goals>
						<configuration>
							<skipTests>${skipIntegrationTests}</skipTests>
							<systemPropertyVariables>
								<logback.configurationFile>${logback-configurationFile}</logback.configurationFile>
							</systemPropertyVariables>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-checkstyle-plugin</artifactId>
				<version>${plugin.version.maven.checkstyle}</version>
				<dependencies>
					<dependency>
						<groupId>com.puppycrawl.tools</groupId>
						<artifactId>checkstyle</artifactId>
						<version>${dependency.version.checkstyle}</version>
					</dependency>
					<dependency>
						<groupId>net.cactusthorn.maven</groupId>
						<artifactId>build-tools</artifactId>
						<version>${project.version}</version>
					</dependency>
				</dependencies>
				<configuration>
					<logViolationsToConsole>true</logViolationsToConsole>
					<configLocation>/checkstyle_checks.xml</configLocation>
					<propertyExpansion>suppressionsFile=/checkstyle_suppressions.xml</propertyExpansion>
				</configuration>
			</plugin>
			<plugin>
				<groupId>com.github.spotbugs</groupId>
				<artifactId>spotbugs-maven-plugin</artifactId>
				<configuration>
					<effort>Max</effort>
					<threshold>Low</threshold>
					<maxRank>20</maxRank>
				</configuration>
			</plugin>
		</plugins>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>com.github.spotbugs</groupId>
					<artifactId>spotbugs-maven-plugin</artifactId>
					<version>${plugin.version.spotbugs.maven}</version>
					<dependencies>
						<dependency>
							<groupId>com.github.spotbugs</groupId>
							<artifactId>spotbugs</artifactId>
							<version>${dependency.version.spotbugs}</version>
						</dependency>
						<dependency>
							<groupId>net.cactusthorn.maven</groupId>
							<artifactId>build-tools</artifactId>
							<version>${project.version}</version>
						</dependency>
					</dependencies>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>
	<scm>
		<url>https://github.com/Gmugra/net.cactusthorn.maven</url>
		<connection>scm:git:https://github.com/Gmugra/net.cactusthorn.maven.git</connection>
	</scm>
</project>